/**
 * {class-name} custom sprite.
 * 
 * @param {Phaser.Game} game
 * @param {Number} x
 * @param {Number} y
 * @param key
 * @param frame
 */
function {class-name}(game, x, y, key, frame) {
	Phaser.Sprite.call(this, game, x, y, key, frame);
	// TODO: generated method.
}

/** @type Phaser.Sprite */
var proto = Object.create(Phaser.Sprite.prototype);
{class-name}.prototype = proto;
{class-name}.prototype.constructor = {class-name};

{class-name}.prototype.update = function() {
	// TODO: generated method.
};