
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.0 (Phaser v2.6.2)


/**
 * Slug
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function Slug(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'atlas', aFrame == undefined || aFrame == null? 'slug/slug-1' : aFrame);
	this.anchor.setTo(0.5, 0.5);
	var _anim_crawl = this.animations.add('crawl', ['slug/slug-1', 'slug/slug-2', 'slug/slug-3', 'slug/slug-4'], 10, true);
	_anim_crawl.play();
	this.game.physics.arcade.enable(this);
	this.body.setSize(20.0, 11.0, 7.0, 10.0);
	this.body.bounce.x = 1.0;
	this.body.velocity.x = 40.0;
	this.body.gravity.y = 500.0;
	
	this.afterCreate();
	
}

/** @type Phaser.Sprite */
var Slug_proto = Object.create(Phaser.Sprite.prototype);
Slug.prototype = Slug_proto;
Slug.prototype.constructor = Slug;

/* --- end generated code --- */

Slug.prototype.afterCreate = function () {
	this.speed = 40;
	this.kind = "slug";
};

Slug.prototype.update = function () {
    if (this.body.velocity.x < 0) {
        this.scale.x = 1;

    } else {
        this.scale.x = -1;

    }
};

Slug.prototype.turnAround = function () {
    if (this.body.velocity.x > 0) {
        this.body.velocity.x = -this.speed;
        this.x -= 5;
    } else {
        this.body.velocity.x = this.speed;
        this.x += 5;
    }
};
