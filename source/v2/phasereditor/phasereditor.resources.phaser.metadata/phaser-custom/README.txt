

Project Templates

- The index.html files of the templates was modified to
reference "lib/phaser.js" (that file is not include in the templates).
- A template has a layout:
		WebContent/
		Design/
		template.json
The phaser files were moved to the WebContent folder. The Design folder is empty because there is not any design file.   




examples-index.html

That file is copied to the examples templates, as index.html.



empty-asset-pack.json

Used by the new asset pack wizard.